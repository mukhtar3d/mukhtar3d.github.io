Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
python-dotenv==1.0.1
requests==2.32.3

# Optional — only needed for the features noted
# openpyxl==3.1.5        # import_universities from .xlsx
# psycopg[binary]==3.1.19 # Postgres instead of SQLite
# gunicorn==22.0.0        # production serving
